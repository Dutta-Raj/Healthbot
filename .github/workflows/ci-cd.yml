name: HealthBot CI/CD Pipeline

on:
  push:
    branches: [ master ]

jobs:
  simple-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Basic file check
      run: |
        echo "=== BASIC CHECK ==="
        ls -la
        echo "Files checked successfully!"

    - name: Success
      run: echo "✅ BASIC CI/CD WORKING!"