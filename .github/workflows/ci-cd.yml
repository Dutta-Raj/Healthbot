name: HealthBot CI/CD

on:
  push:
    branches: [ master ]

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Validate files
      run: |
        echo "✅ HealthBot CI/CD - Basic Validation"
        echo "📁 Files checked successfully!"