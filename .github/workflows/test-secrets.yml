name: Test Secrets Access

on:
  workflow_dispatch:  # Allows manual trigger

jobs:
  test-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Test if secrets exist
        run: |
          echo "Repository secrets test:"
          echo "SECRET_1 exists: ${{ secrets.#EATHS007 != '' }}"
          echo "Number of chars in SECRET_1: ${{ length(secrets.#EATHS007) }}"
          
      - name: Test secret in action (masked)
        run: |
          echo "Using secret: ${{ secrets.#EATHS007 }}"
          # The value will be masked with *** in logs
